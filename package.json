{
  "name": "devbarometer",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "prepare": "husky",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run start",
    "type-check": "npm --workspace frontend run type-check && npm --workspace backend run type-check",
    "lint": "npm --workspace frontend run lint && npm --workspace backend run lint",
    "format": "npm --workspace frontend run format && npm --workspace backend run format",
    "format:check": "npm --workspace frontend run format:check && npm --workspace backend run format:check",
    "test": "npm --workspace frontend run test && npm --workspace backend run test",
    "test:coverage": "npm --workspace frontend run test:coverage && npm --workspace backend run test:coverage"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1"
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": ["npm --workspace frontend run lint", "npm --workspace frontend run test:changed"],
    "backend/src/**/*.{ts,tsx}": ["npm --workspace backend run lint", "npm --workspace backend run test:changed"]
  },
  "engines": {
    "node": ">=22"
  }
}
